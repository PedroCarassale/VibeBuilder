# VibeBuilder Android (Delivery 1 Skeleton)

This repository contains a simple first functional Android app skeleton for Delivery 1.

Implemented screens:
- Project list
- Create project
- Prompt input / iteration
- Generated app preview placeholder
- Version history placeholder

Architecture is prepared with a `ProjectRepository` interface and `MockProjectRepository` so backend API integration can be added later without changing UI screen contracts.
